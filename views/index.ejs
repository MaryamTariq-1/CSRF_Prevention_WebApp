<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSRF Prevention Project</title>
    <link rel="stylesheet" href="/public/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand text-white" href="#hero">CSRF Prevention</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link text-white" href="#hero">Home</a></li>
                    <li class="nav-item"><a class="nav-link text-white" href="#about">About</a></li>
                    <li class="nav-item"><a class="nav-link text-white" href="#features">Features</a></li>
                    <li class="nav-item"><a class="nav-link text-white" href="#signup">Sign Up</a></li>
                    <li class="nav-item"><a class="nav-link text-white" href="#csrf-demo">CSRF Demo</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section id="hero" class="hero-section text-center py-5 text-white" style="background-color: #b6970f;">
        <div class="container">
            <h1 class="display-4">Welcome to CSRF Prevention</h1>
            <p class="lead">A solution to protect against Cross-Site Request Forgery (CSRF) attacks</p>
        </div>
    </section>
    

    <!-- About Section -->
    <section id="about" class="about-section py-5">
        <div class="container">
            <h2 class="text-center mb-4">About This Project</h2>
            <p class="text-center">This project demonstrates how to prevent CSRF attacks using modern techniques like anti-CSRF tokens, same-site cookies, and referrer validation. It is built with Node.js, Express.js, and Bootstrap for a secure and interactive experience.</p>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="features-section py-5 bg-light">
        <div class="container">
            <h2 class="text-center mb-4">Key Features</h2>
            <div class="row">
                <!-- Card 1 -->
                <div class="col-md-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body text-center">
                            <h4 class="card-title">CSRF Token Validation</h4>
                            <p class="card-text">Ensures every form submission is secure with unique tokens.</p>
                        </div>
                    </div>
                </div>
                <!-- Card 2 -->
                <div class="col-md-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body text-center">
                            <h4 class="card-title">Same-Site Cookies</h4>
                            <p class="card-text">Prevents cross-origin attacks by setting strict cookie policies.</p>
                        </div>
                    </div>
                </div>
                <!-- Card 3 -->
                <div class="col-md-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body text-center">
                            <h4 class="card-title">Referrer Validation</h4>
                            <p class="card-text">Validates request origins to prevent unauthorized actions.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    

    <!-- Signup Section -->
    <section id='signup' class="auth-section py-5">
        <div class="container">
            <h2 class="text-center mb-4">Sign Up</h2>
            <div class="row justify-content-center">
                <!-- Sign Up Form -->
                <div class="col-md-6 col-lg-4">
                    <form action="/signup" method="POST" class="p-4 border rounded bg-white shadow">
                        <!-- CSRF Token -->
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        
                        <!-- Name -->
                        <div class="mb-3">
                            <label for="signup-name" class="form-label">Name</label>
                            <input type="text" class="form-control" id="signup-name" name="name" required>
                        </div>

                        <!-- Email -->
                        <div class="mb-3">
                            <label for="signup-email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="signup-email" name="email" required>
                        </div>

                        <!-- Password -->
                        <div class="mb-3">
                            <label for="signup-password" class="form-label">Password</label>
                            <input type="password" class="form-control" id="signup-password" name="password" required>
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Sign Up</button>
                    </form>
                </div>
            </div>
        </div>
    </section>

     <!-- CSRF Demo Section -->
     <section id='csrf-demo' class="py-5">
        <div class="container">
            <h2 class="text-center mb-4">CSRF Protection Demo</h2>
            <form action="/simulate" method="POST" class="p-4 border rounded bg-white shadow mx-auto" style="max-width: 500px;">
                <!-- CSRF Token -->
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                
                <div class="mb-3">
                    <label for="data" class="form-label">Enter Data</label>
                    <input type="text" class="form-control" id="data" name="data" required>
                </div>

                <button type="submit" class="btn btn-primary w-100">Submit</button>
            </form>
        </div>
    </section>

    <!-- Footer Section -->
    <footer class="footer py-3 bg-dark text-white text-center">
        <div class="container">
            <p>&copy; 2024 CSRF Prevention Project. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Smooth scroll for navigation links
        document.querySelectorAll('a.nav-link').forEach(link => {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                const target = document.querySelector(link.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
    </script>
</body>
</html>
